use super::InsertVariableBody;
use crate::table::input::Field;

impl InsertVariableBody {
    /// Create the function body to generate from `fields` and `table_name`
    pub fn from_input(fields: &[Field], table_name: &str) -> Self {
        todo!();
    }
}
