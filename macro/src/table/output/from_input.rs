use super::Output;
use crate::table::{Input, TableFlags};

impl Output {
    /// Create the structures to generate from `input`
    pub fn from_input(input: Input, flags: TableFlags) -> Self {
        Output {}
    }
}
